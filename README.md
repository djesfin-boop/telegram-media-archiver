# 📸 Telegram Media Archiver

> Бот для архивации медиа-групп (альбомов фотографий) из Telegram каналов и чатов с сохранением на локальный диск

## 🎯 Возможности

- ✅ **Автоматическая архивация** медиа-групп (альбомов фото)
- ✅ **Сохранение описаний** к фотографиям
- ✅ **Локальное хранение** - все файлы на вашем диске
- ✅ **Метаданные** - информация о группах в JSON формате
- ✅ **Организованная структура** папок
- ✅ **Статистика** по сохранённым файлам

## 📁 Структура сохранённых файлов

```
downloads/
├── metadata.json          # Общий индекс всех групп
└── media_groups/
    ├── group_0001/
    │   ├── photo_01.jpg
    │   ├── photo_02.jpg
    │   ├── photo_03.jpg
    │   └── info.json      # Метаданные группы
    ├── group_0002/
    │   └── ...
    └── ...
```

## 🚀 Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone https://github.com/djesfin-boop/telegram-media-archiver.git
cd telegram-media-archiver
```

### 2. Установить зависимости

```bash
pip install -r requirements.txt
```

### 3. Настроить бота

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Скопируйте токен бота
3. Создайте файл `.env` из шаблона:

```bash
cp .env.example .env
```

4. Откройте `.env` и добавьте свой токен:

```env
BOT_TOKEN=your_bot_token_here
DOWNLOAD_DIR=downloads
```

### 4. Запустить бота

```bash
python bot.py
```

## 📝 Использование

1. **Запустите бота** - отправьте `/start`
2. **Отправьте фото** - одиночное или альбом
3. **Получите подтверждение** - бот сообщит о сохранении
4. **Проверьте статистику** - команда `/stats`

### Команды бота

- `/start` - Приветствие и инструкция
- `/stats` - Статистика сохранённых файлов

## ⚖️ Соответствие правилам

### ✅ Telegram API

- Использует официальный Bot API
- Соблюдает rate limits (20 запросов/сек)
- Работает только с доступным контентом
- Для приватных каналов требуется добавление бота

### ✅ GitHub

- **Код проекта** - хранится в репозитории
- **Медиа файлы** - НЕ хранятся (в `.gitignore`)
- **Токены** - НЕ коммитятся (в `.gitignore`)
- **Локальные данные** - только на вашем диске

## 📂 Где хранятся данные

### ❌ Не в GitHub
- Скачанные фото/видео
- Токены и API ключи
- Базы данных с контентом
- Логи работы

### ✅ На локальном диске
- `downloads/media_groups/` - все медиа
- `downloads/metadata.json` - индекс групп
- `.env` - токены (создаёте вручную)

## 🛡️ Безопасность

- **Токены** хранятся только локально в `.env`
- **Файл `.env`** добавлен в `.gitignore`
- **Никакие секреты** не попадут в репозиторий
- **Все загрузки** остаются на вашем компьютере

## 🔧 Требования

- Python 3.8+
- Telegram Bot Token (от @BotFather)
- ~50 МБ свободного места (на тысячу фото)

## 📦 Зависимости

```
python-telegram-bot==21.0
python-dotenv==1.0.1
aiofiles==23.2.1
```

## 🤝 Вклад

Пул реквесты приветствуются! Для больших изменений сначала откройте issue.

## 📄 Лицензия

MIT

## 📧 Контакты

Вопросы и предложения - через Issues на GitHub
